namespace xp.tdd.bowling.game.service
{
    public static class GameConfiguration
    {
        public const int MIN_FRAME_LIMIT = 1;
        public const int MAX_FRAME_LIMIT = 10;
        public const int MAX_ROLL_LIMIT = 2;
        public const int MAX_ROLL_SCORE = 10;
    }

}